export default function App() {
  return (
    <main className="app-shell">
      <section className="mission-panel" aria-labelledby="mission-heading">
        <p className="eyebrow">Adaptive Coach</p>
        <h1 id="mission-heading">Today's Mission</h1>
        <p className="mission-copy">
          Your plan will adapt around recovery, weather, race timing, and the
          training you have already absorbed.
        </p>
      </section>
    </main>
  );
}
